# Gate Decisions — LaunchGuard

## Gate A — 2026-06-20
Decision: Continue (lead pre-delegated full authority; no pause requested — orchestrator presents summary for the record, build proceeds)
Spec-validation: Validated against the real worknote-ai SECRET_FOO/secretAccessor scenario. worknote-ai's `infra/cloud-run/service.yaml` declares 9 `secretKeyRef` secrets (JWT_SECRET_KEY, SES_SMTP_*, SENTRY_DSN_BACKEND, LITELLM_*, CLAMAV_FUNCTION_URL). The grant of `roles/secretmanager.secretAccessor` is provisioned by `infra/gcp/secrets-setup.sh` (operator-only, NOT run in CI). If a new secretKeyRef is added to service.yaml but secrets-setup.sh is not re-run for it, deploy succeeds and the first request 500s on secret mount — exactly LaunchGuard's `secret-ref-without-secretAccessor` will-fail killer. The spec's RepoAuditor (reads secret_refs) + GcpStateInspector (reads secrets[].accessor_members) + Reconciler (diffs them) would catch this on real artifacts. PORT (Dockerfile ENV PORT=8080 / EXPOSE 8080 vs service.yaml containerPort 8080) and probes (/health, /ready) also map to real, parseable fields. Spec direction confirmed against reality.
Notes: All Phase 1 artifacts written and validated — PRD (8 sections, G/W/T), 8 FRDs, architecture.md, api-contracts.yaml (spectral lint 0 errors), implementation-tasks.json (17 tasks, drift audit clean), deployment-requirements.md, 3 ADRs. NEW_SERVICE: postgres-pgvector flagged → devops-local INFRA-01 before engineers. Pattern 2 (AI feature): llm-engineer before backend-engineer. AI Operating Principles treated as required guardrail input throughout. Memory (P2) + canary (stretch) flagged as first-to-cut.

## Gate B — 2026-06-21 (Phase 2, Increment 1 — spine through the Reconciler, Plan days 1–4)
Decision: Deploy/Continue (lead pre-delegated full authority; no pause requested — orchestrator records the decision and self-verifies). Increment 1 accepted; Increment 2 (FixWriter PR generation, full eval matrix, memory recall, canary, video) proceeds next.
Real-data UAT: N/A as a deployed user-facing flow (LaunchGuard is a CLI/ADK agent; `adk web` demo trace is DEMO-01, Increment 2 — requires google-adk which could not be installed in this sandbox). Substituted real-data validation: the hero fixture `fixtures/repos/worknote-ai-like/` + `fixtures/gcp/worknote-ai-like.json` are shaped from the REAL worknote-ai service.yaml (9 actual secretKeyRef names, port 8080, /health+/ready probes, python:3.12-slim, min0/max3, concurrency80). The full spine was run end-to-end in fixture mode over this realistic snapshot and the `secret-ref-without-secretAccessor` will-fail KILLER fired on JWT_SECRET_KEY (confidence 0.98, verdict BLOCKED), with zero false positives on the other 8 secrets that hold the accessor grant. Eval scorecard: caught 1/1 blockers, precision 1.00 / recall 1.00. This is the production-shaped-data evidence for the increment; live `adk web` UAT is the Increment-2 gate.
Consumption wiring: the three-source spine is fully wired and its effect is asserted (RepoAuditor→Intended, Declared parser→Declared, GcpStateInspector/fixture→Live → Reconciler → classified ReconciliationDelta → ReadinessScorecard). The downstream CONSUMER of the deltas — FixWriter `propose_patch`/`open_pr` (PR generation) — is DEFERRED to Increment 2 with owning task IDs BE-06 + LLM-04 (explicit deferral per workflow-discipline "Consumption wiring"). The scorecard (terminal artifact) is produced and asserted now via the eval harness.
E2E coverage: the eval harness IS the E2E surface for this project (architecture §7). `eval/run_eval.py` + `tests/test_e2e_eval.py` run the full spine in fixture mode green; determinism (replay x2 byte-identical) and zero-network/zero-subprocess are asserted. No CI workflow exists yet (noted in local-ci.sh header) — `make verify` is the green gate, run authoritatively by the orchestrator: ruff clean, mypy clean, 187 pytest passed.
Notes: Network-constrained sandbox — `google-adk`/`google-genai` could NOT be pip-installed (timeout); deterministic-core design (model-at-edges) meant zero impact on the Increment-1 deliverable, which is pure Python + stdlib + pyyaml. Test toolchain (ruff/mypy/pytest) DID install. Code review CONDITIONAL, 9/9 critical security checks PASS, zero blocking findings; non-blocking findings (fragile test assertion, redact "text"-passthrough self-documentation, ruff E501-global) carried to Increment 2. LLM-01 reconciled to `tested` with 18 dedicated skeleton-acceptance tests. Loop counters all 0 (no fix/hotfix/replan cycles needed). Stash sentinel clean throughout.
